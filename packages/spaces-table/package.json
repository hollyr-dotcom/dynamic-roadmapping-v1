{
  "name": "@spaces/spaces-table",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "start": "vite preview --host 0.0.0.0 --port $PORT",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@spaces/shared": "file:../shared",
    "simple-icons": "^16.12.0"
  }
}
