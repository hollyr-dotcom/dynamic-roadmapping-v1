{
  "name": "@spaces/spaces-table",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@spaces/shared": "workspace:*",
    "simple-icons": "^16.12.0"
  }
}
